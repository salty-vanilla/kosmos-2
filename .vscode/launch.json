{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Invoke: demo",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/.venv/bin/invoke",
            "args": ["demo"],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
